function User(id, username, email, profile) {
  this.id = id;
  this.username = secret;
  this.password = userId;
  this.email = email;
  this.profile = profile;
  this.devices = [];
}

module.exports = ClientSchema;
